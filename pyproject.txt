[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "hqp"
version = "0.1.0"
description = "A pathfinding algorithm for navigating mazes with dynamic obstacles."
authors = [
    { name = "Vladlen", email = "vladlen.codes@gmail.com" }
]
license = { file = "LICENSE" }
readme = "README.md"
requires-python = ">=3.8"
keywords = ["pathfinding", "maze", "algorithm", "dynamic obstacles"]
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

[project.urls]
homepage = "https://github.com/vladlen-codes/HQP"
repository = "https://github.com/vladlen-codes/HQP"

[tool.setuptools.packages.find]
where = ["src"]
include = ["hqp*"]
